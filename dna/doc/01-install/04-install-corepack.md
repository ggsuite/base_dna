## Install corepack

Make sure Node is installed, on
[Mac](./03-install-node-mac.md) or on [Windows](./03-install-node-win.md)

On Windows:

Press `Windows`

Type `Cmd`

Select `Open as administrator`

Execute the the following commands

```bash
npm install --global corepack@latest
corepack enable pnpm
```